# 发布 Normalization 文章并修正上下篇方向设计

## 目标

只发布《05 Normalization（归一化）》，确保文章、数学公式和配图在线正常展示；同时修正文章页底部导航，使“上一篇”指向编号更小的文章，“下一篇”指向编号更大的文章。

## 发布范围

- 将 `source/_posts/blog/大模型llm/05 Normalzation（归一化）.md` 的 `published` 改为 `true`。
- 将该文章加入 `_config.yml` 的 `published_posts` 白名单。
- 保留该文章在 Hexo 静态资源目录和 Obsidian 镜像目录中的语义化图片副本。
- 不发布或提交 `06` 及之后的草稿，也不提交工作区中的其他无关文章改动。

## 导航行为

使用 Butterfly 已提供的 `post_pagination` 配置，不修改主题模板：

- 将 `_config.butterfly.yml` 中的 `post_pagination` 从 `1` 改为 `2`。
- “上一篇”链接到发布时间更早、编号更小的文章。
- “下一篇”链接到发布时间更新、编号更大的文章。
- 当前只发布到 `05` 时，`04` 页面应显示“下一篇 05”，`05` 页面应显示“上一篇 04”，且 `05` 暂无下一篇。

## 验证与发布

1. 使用发布白名单生成站点，确认仅有 `01` 至 `05` 五篇文章。
2. 检查 `05` 的生成页面包含正文、公式渲染配置和图片引用，图片文件存在且可读取。
3. 检查 `04` 与 `05` 生成页面中的上下篇标签、标题和链接方向。
4. 只提交本次发布所需的文章、图片、白名单脚本与配置、导航配置和说明文档。
5. 推送源码到远端 `source`，再通过现有安全发布脚本将生成站点正常推送到 `main`，不强制推送。
6. 在线访问文章页面与图片地址，确认 HTTP 状态和关键内容正常。


